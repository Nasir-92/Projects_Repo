<UserControl x:Class="EcoBite.Views.RecuperarCuenta"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:vm="clr-namespace:EcoBite.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="900"
    d:DesignWidth="900">

    <UserControl.DataContext>
        <vm:RecuperarCuentaVM/>
    </UserControl.DataContext>

    <UserControl.Resources>

        <Style x:Key="FormLabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="#2E7D32"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="FormTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="#4CAF50"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Margin" Value="0,5,0,20"/>
        </Style>

        <Style x:Key="MainButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
        </Style>

    </UserControl.Resources>

    <Grid>

        <!-- Fondo -->
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/fondo.png"
                        Stretch="UniformToFill"/>
        </Grid.Background>

        <!-- Botón volver -->
        <Button Content="←"
                Width="40"
                Height="40"
                FontSize="20"
                FontWeight="Bold"
                Background="#5ECF85"
                Foreground="White"
                BorderBrush="#4CAF50"
                BorderThickness="2"
                Cursor="Hand"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="20"
                Command="{Binding VolverCommand}"/>

        <!-- Tarjeta central -->
        <Border Width="500"
                Padding="40"
                CornerRadius="25"
                Background="#CCFFFFFF"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <StackPanel>

                <!-- Logo -->
                <Image Source="/Assets/logo.png"
                       Width="90"
                       Height="90"
                       Margin="0,0,0,20"
                       HorizontalAlignment="Center"/>

                <!-- Título -->
                <TextBlock Text="Recuperar cuenta"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="#2E7D32"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>

                <!-- Texto explicativo -->
                <TextBlock Text="Introduce tu correo electrónico y te enviaremos instrucciones para recuperar tu cuenta."
                           TextWrapping="Wrap"
                           FontSize="14"
                           Margin="0,0,0,20"
                           TextAlignment="Center"/>

                <!-- Email -->
                <TextBlock Text="Correo electrónico"
                           Style="{StaticResource FormLabelStyle}"/>

                <TextBox Style="{StaticResource FormTextBoxStyle}"
                         Text="{Binding Email}"/>

                <!-- Botón -->
                <Button Content="Enviar enlace de recuperación"
                        Style="{StaticResource MainButtonStyle}"
                        Command="{Binding EnviarCodigoCommand}"/>

                

            </StackPanel>

        </Border>

    </Grid>

</UserControl>